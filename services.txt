# systemd services to enable at boot.
# install.sh runs: sudo systemctl enable <service>
# (without --now: they start on the next reboot. On a running machine
# `enable` is idempotent and leaves live processes alone.)

NetworkManager.service
bluetooth.service
ufw.service
