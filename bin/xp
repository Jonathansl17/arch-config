#!/bin/bash
if [ $# -eq 0 ]; then
    echo "usage: xp file.xopp [file2.xopp ...]" >&2
    exit 1
fi
for f in "$@"; do
    if [ ! -e "$f" ]; then
        echo "xp: '$f' does not exist" >&2
        continue
    fi
    /usr/bin/xournalpp --create-pdf "${f%.*}.pdf" "$f"
done
