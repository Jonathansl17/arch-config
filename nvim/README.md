# nvim

Neovim + IdeaVim config.

## Files

- `init.lua`   → `~/.config/nvim/init.lua`
- `ideavimrc`  → `~/.ideavimrc` (vim emulation in JetBrains IDEs)

## Notes

Single-file Lua config — no plugin manager, no lazy/packer. Keep it small.
