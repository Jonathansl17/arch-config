# AUR packages, explicitly installed.
# Install: yay -S --needed - < aur.txt
# install.sh bootstraps yay if missing, then installs this list.
#
# yay and yay-debug are intentionally excluded: yay is bootstrapped in
# install.sh before this file is read, and yay-debug is a build byproduct.
#
# lemonbar-xft-git is intentionally NOT listed here: it needs CC=gcc to build
# (clang rejects -march=x86-64). install.sh handles it in its own section.
#
# pnpm is intentionally NOT listed here: pnpm-bin is orphaned in AUR, so we
# install it via the official curl script in install.sh instead.

android-sdk-cmdline-tools-latest
brave-bin
burpsuite
freeoffice
intellij-idea-ultimate-edition
jmeter
k6
ngrok
pgadmin4-desktop-bin
pgadmin4-server-bin
postman-bin
python312
ttf-ms-fonts
visual-studio-code-bin
